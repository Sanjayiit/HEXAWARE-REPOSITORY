1).UPDATE products SET price = 800 WHERE product_id = 3;
2).Delete from cart where customer_id = 1;
3).select  product_id, name,description, stockQuantity from products where price<100;
4). select product_id,name from products where  stockQuantity >5;
5).select  order_id, customer_id from  orders where  total_price BETWEEN 500 AND 1000;
6).select * from products where name LIKE '%r';
7). select cart_id,customer_id,product_id,quantity from cart where customer_id = 2;
8).select customers.customer_id,customers.first_name,customers.last_name,customers.address,orders.order_id from customers JOIN orders ON customers.customer_id = orders.customer_id where YEAR(orders.order_date) = 2023;
9).select name,min(stockQuantity) from products GROUP BY name;
10). select customers.first_name,customers.last_name,SUM(orders.total_price) from customers JOIN orders ON customers.customer_id = orders.customer_id GROUP BY customers.first_name,customers.last_name;
11). select  customers.first_name,customers.last_name,AVG(orders.total_price) from customers JOIN orders ON customers.customer_id = orders.customer_id GROUP BY customers.first_name,customers.last_name;
12). select customers.first_name,customers.last_name,COUNT(orders.order_id) from customers JOIN orders ON customers.customer_id = orders.customer_id GROUP BY customers.first_name,customers.last_name;
13). select customers.first_name,customers.last_name,MAX(orders.total_price) from customers JOIN orders ON customers.customer_id = orders.customer_id GROUP BY customers.first_name,customers.last_name;
14).select customers.customer_id, customers.first_name, customers.last_name, SUM(orders.total_price) from customers JOIN orders ON customers.customer_id = orders.customer_id GROUP BY customers.customer_id, customers.first_name, customers.last_name HAVING SUM(orders.total_price) > 1000;
15). select  name,price from products where product_id NOT IN( select product_id from cart);
16). select first_name,last_name from customers where customer_id NOT IN (select customer_id from orders);
17). select products.product_id,products.name,SUM(order_items.item_Amount),(SUM(order_items.item_Amount)*100/(select SUM(order_items.item_Amount)from order_items))from products JOIN order_items ON products.product_id = order_items.product_id GROUP BY products.product_id,products.name;
18). select product_id,name from products where stockQuantity IN ( select min(stockQuantity) from products);
19).select first_name,last_name ,email, address from customers where customer_id IN  ( select customer_id from orders where total_price = (select MAX(total_price) from orders));